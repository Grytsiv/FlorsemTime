import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    avatar: {
        marginLeft: 10,
    },
    email: {
        marginLeft: 10,
    },
    logOutButton: {
        marginLeft: 10,
        marginRight: 10,
    },
});

export default styles;
